#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int n, s; cin >> n >> s;
    if (n > 1) {
        cout << 4 * s * s << '\n';
        return 0;
    }
    int a, b, c, d; cin >> a >> b >> c >> d;
    if (-s <= a && a <= s && -s <= b && b <= s && )
    return 0;
}