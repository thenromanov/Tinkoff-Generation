#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int n, h, b; cin >> n >> b >> h;
    vector <vector <int>> dp(h + 1, vector <int> (n + 1));
    return 0;
}